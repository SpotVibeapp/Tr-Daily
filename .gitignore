# Flutter / Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
.metadata
pubspec.lock          # regenerated by `flutter pub get` (this repo ships lib-only)

# Stock counter test dropped in by `flutter create .` (see README step 3).
# It builds `MyApp`, which does not exist here — the app class is `TrDailyApp`
# and the real widget test is test/app_smoke_test.dart. Delete it locally.
test/widget_test.dart

# Platform folders — regenerate locally with: flutter create .
android/
ios/
linux/
macos/
windows/
web/

# IDE
.idea/
*.iml
.vscode/*
!.vscode/launch.json

# Local secrets / env
.env
*.keys.json

# Python leftovers (abandoned stack)
.venv/
__pycache__/

# OS
.DS_Store
